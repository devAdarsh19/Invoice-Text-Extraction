 # for idx in range(len(merged_result)):
    #     res = merged_result[idx]
    #     box, (text, conf) = res
    #     x1, y1, x2, y2 = box[0][0], box[0][1], box[2][0], box[2][1]
    #     w, h = x2 - x1, y2 - y1
    #     # cv2.boundingRect(img_path, (x1, y1), (x1 + w, y1 + h), (0, 255, 0), 2)
    #     print()